def proc(x):
    return 2*x*x
def main():
    x = 10
    print(proc(x))
main()

# yeah the program is legel since the scope of variable x is Local and exclusive with in the scope of each defined function.