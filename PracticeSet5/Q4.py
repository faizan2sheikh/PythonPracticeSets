def proc(x):
    return 2*x*x
y = 10
print(proc(y))

# This program is legal the argument that is defined in a function just tells the type of data needed as argument, though the data its
# thats why we can use y instead of x